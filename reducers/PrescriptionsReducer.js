import prescriptions from './prescriptions.json'

const initialState = {
  prescriptions,
};

export default (state = initialState, action ) => {
  switch (action.tyle) {
    default:
      return state
  }
}
